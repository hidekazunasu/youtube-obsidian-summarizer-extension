{
  "name": "youtube-obsidian-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp manifest.json dist/ && cp public/icon-128.png dist/",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.307",
    "typescript": "^5.7.3",
    "vite": "4.5.5",
    "vitest": "0.34.6"
  }
}
